{% extends "base.html.twig" %}

{% block title '[Mon site]' %}

{% block body %}
    <div class="jumbotron mt-4">
        <h1 class="text-center">Liste des offres</h1>
        <hr class="my-5">
        <div class="row">
            <div class="col-4 text-center">
                <a href="{{ path('app_vehicle_sale') }}">Vente</a>
            </div>
            <div class="col-4 text-center">
                <a href="{{ path('app_vehicle_rent') }}">Location</a>
            </div>
            <div class="col-4 text-center">
                <a href="{{ path('app_vehicle') }}">Voir tout</a>
            </div>
        </div>
    </div>
{% endblock %}
